import { storeUtilFormatter } from './store-util-formatter';

describe('storeUtilFormatter', () => {
  it('should work', () => {
    expect(storeUtilFormatter()).toEqual('store-util-formatter');
  });
});
